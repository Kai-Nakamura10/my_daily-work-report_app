<div class="text-center py-5">
  <h1 class="display-3 fw-bold">業務日報アプリ</h1>
  <p class="lead">毎日の仕事をスマートに記録しましょう。</p>
  <% if !logged_in? %>
    <%= link_to "ユーザー登録", signup_path, class: "btn btn-primary btn-lg mt-3" %>
  <% else %>
    <%= link_to "日報一覧へ", reports_path, class: "btn btn-success btn-lg mt-3" %>
  <% end %>
</div>